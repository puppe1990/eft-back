<%= form_for(@custom_phrase) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  
  <%= f.select :phrase_ids, options_for_select(Phrase.all.map {|p| [p.description, p.id]}, @custom_phrase.phrase_ids), {}, multiple: true %>
  
  <%= f.submit "Update Custom Phrase" %>
<% end %> 